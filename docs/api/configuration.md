# Configuration

Scientific hyperparameters in `AdaptConfig` are frozen for the version-1 method. Target-specific changes are rejected rather than silently accepted.

::: hyperspatial.config
    options:
      show_root_heading: true
      show_source: false
