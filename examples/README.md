# Examples

`minimal_2d` and `minimal_3d` are deterministic synthetic fixtures generated by `scripts/make_tiny_example.py`. They exercise the real public API without redistributing manuscript data. They are intentionally small and are not biological evidence.

